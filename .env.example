# Database (Legacy Prisma - will be replaced by Supabase)
DATABASE_URL="postgresql://gamma:gamma_dev_password@localhost:5432/gamma_klone?schema=public"

# Supabase (Production database + Auth)
NEXT_PUBLIC_SUPABASE_URL="https://yeaxjmilwjhudojybqak.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."  # Get from Supabase Dashboard > Settings > API
SUPABASE_DB_PASSWORD="your-db-password"  # Get from Supabase Dashboard > Settings > Database

# Redis
REDIS_URL="redis://localhost:6379"

# Image Generation Provider
# Options: "gemini" (default, Nano Banana Pro) | "openai" (DALL-E 3)
IMAGE_PROVIDER="gemini"

# Gemini AI (for image generation - Nano Banana Pro)
GEMINI_API_KEY="AIza..."
GEMINI_IMAGE_MODEL="gemini-3-pro-image-preview"
GEMINI_ASPECT_RATIO="16:9"
GEMINI_IMAGE_SIZE="2K"

# OpenAI (fallback for images, or primary for text generation)
OPENAI_API_KEY="sk-..."

# S3-kompatibel lagring (MinIO, AWS S3, etc.)
S3_ENDPOINT="http://localhost:9000"
S3_ACCESS_KEY="minioadmin"
S3_SECRET_KEY="minioadmin"
S3_BUCKET="gamma-klone"
S3_REGION="us-east-1"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
APP_SECRET="change-this-in-production"

# Rate Limiting
RATE_LIMIT_PER_MINUTE=10
RATE_LIMIT_PER_DAY=100

# Worker
WORKER_CONCURRENCY=2

# Export URLs expiry (seconds)
EXPORT_URL_EXPIRY=3600

# Testing
FAKE_LLM="false"
